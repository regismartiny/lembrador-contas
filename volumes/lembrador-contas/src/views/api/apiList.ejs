<%- include ('../partials/header.ejs') %>

    <body>
        <%- template.navbar %>
            <div class="container">
                <div class="starter-template">
                    <div class="list-page-header">
                        <h1 class="title-1"><%= title %></h1>
                        <a href='/apis/new' class="btn btn-outline-success btn-sm">Novo</a>
                    </div>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th>Situação</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% apiList.forEach(function(api){ %>
                                <tr>
                                    <td><%= api.name %></td>
                                    <td><%= statusEnum[api.status] %></td>
                                    <td class="text-nowrap">
                                        <a href='/apis/edit/<%= api._id %>'><span class="material-symbols-outlined">edit</span></a>
                                        <form action="/apis/remove/<%= api._id %>" method="post" style="display:inline"><input type="hidden" name="_csrf" value="<%= csrfToken %>"><button type="submit" class="btn-link" onclick="return confirm('Excluir?')"><span class="material-symbols-outlined symbol-red">delete</span></button></form>
                                    </td>
                                </tr>
                            <%})%>
                        </tbody>
                    </table>
                </div>
            </div>
            <%- template.scripts %>
    </body>

    </html>
