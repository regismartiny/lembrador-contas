<%- include ('../partials/header.ejs') %>

    <body>
        <%- template.navbar %>
            <div class="container">
                <div class="starter-template">
                    <div class="list-page-header">
                        <h1 class="title-1"><%= title %></h1>
                        <a href='/tables/new' class="btn btn-outline-success btn-sm">Novo</a>
                    </div>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th>Situação</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% tableList.forEach(function(table){ %>
                                <tr>
                                    <td><%= table.name %></td>
                                    <td><%= statusEnum[table.status] %></td>
                                    <td class="text-nowrap">
                                        <a href='/tables/edit/<%= table._id %>'><span class="material-symbols-outlined">edit</span></a>
                                        <a href='/tables/remove/<%= table._id %>'><span class="material-symbols-outlined symbol-red">delete</span></a>
                                    </td>
                                </tr>
                            <%})%>
                        </tbody>
                    </table>
                </div>
            </div>
            <%- template.scripts %>
    </body>

    </html>
