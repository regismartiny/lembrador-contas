<%- template.head %>

    <body>
        <%- template.navbar %>
            <div class="container">
                <div class="float-right">
                    <form class="form-inline my-2 my-lg-0">
                        <a href='/tables/new' class="btn btn-outline-success my-2 my-sm-0">Novo</a>
                    </form>
                </div>
                <div class="starter-template">
                    <div>
                        <h1 class="title-1">
                            <%= title %>
                        </h1>
                        <br>
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Nome</th>
                                    <th>Situação</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% tableList.forEach(function(table){ %>
                                    <tr>
                                        <td>
                                            <%= table.name %>
                                        </td>
                                        <td>
                                            <%= statusEnum[table.status] %>
                                        </td>
                                        <td>
                                            <a href='/tables/edit/<%= table._id %>'>
                                                <span class="material-symbols-outlined">
                                                edit
                                                </span>
                                            </a>
                                            <a href='/tables/remove/<%= table._id %>'>
                                                <span class="material-symbols-outlined">
                                                delete
                                                </span>
                                            </a>
                                        </td>
                                    </tr>
                                    <%})%>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <%- template.scripts %>
    </body>

    </html>